<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizappnew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizAppNew">
        <activity android:name=".activities.DatabaseEditor" android:windowSoftInputMode="adjustPan" android:screenOrientation="landscape"/>
        <activity android:name=".activities.Score" android:screenOrientation="landscape"/>
        <activity android:name=".activities.Highscore" android:screenOrientation="landscape"/>
        <activity android:name=".activities.Play" android:screenOrientation="landscape"/>
        <activity android:name=".activities.Menu" android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>